services:
  - type: web
    name: send-email-web
    env: java
    region: singapore
    plan: free
    root: .
    buildCommand: mvn clean package -DskipTests
    startCommand: java -Xmx512m -Xms256m -jar target/dependency/webapp-runner.jar --port $PORT target/send-email.war
